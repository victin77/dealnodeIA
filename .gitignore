# dependências
node_modules/

# builds
dist/

# variáveis de ambiente (NUNCA versionar)
.env
.env.local
.env.*.local

# banco SQLite (legado / local)
*.db
*.db-journal

# uploads e logs
server/uploads/*
!server/uploads/.gitkeep
*.log

# arquivo de referência de design (uso interno)
design-ref.jpg

# sistema operacional
.DS_Store
Thumbs.db
